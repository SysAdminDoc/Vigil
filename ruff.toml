target-version = "py312"
line-length = 100
exclude = [
  "build",
  "chromium_src",
  "ungoogled-chromium",
  ".pytest_cache",
  ".ruff_cache",
]

[lint]
select = ["E4", "E7", "E9", "F", "W"]

[lint.per-file-ignores]
"branding/generate_icons.py" = ["E402", "F401", "F541"]
