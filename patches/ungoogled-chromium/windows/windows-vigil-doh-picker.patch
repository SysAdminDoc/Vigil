--- a/chrome/browser/net/default_dns_over_https_config_source.cc
+++ b/chrome/browser/net/default_dns_over_https_config_source.cc
@@ -25,7 +25,12 @@ DefaultDnsOverHttpsConfigSource::Default
   if (set_up_pref_defaults) {
     local_state->SetDefaultPrefValue(prefs::kDnsOverHttpsMode,
                                      base::Value(SecureDnsConfig::ModeToString(
-                                         net::SecureDnsMode::kOff)));
+                                         net::SecureDnsMode::kSecure)));
+    local_state->SetDefaultPrefValue(
+        prefs::kDnsOverHttpsTemplates,
+        base::Value("https://dns.quad9.net/dns-query"));
+    local_state->SetDefaultPrefValue(
+        prefs::kDnsOverHttpsAutomaticModeFallbackToDoh, base::Value(false));
   }
 }
--- a/net/dns/public/doh_provider_entry.cc
+++ b/net/dns/public/doh_provider_entry.cc
@@ -207,9 +207,37 @@ const DohProviderEntry::List& DohProvide
        {"NextDns",
        MAKE_BASE_FEATURE_WITH_STATIC_STORAGE(DohProviderNextDns,
                                              base::FEATURE_ENABLED_BY_DEFAULT),
        /*dns_over_53_server_ip_strs=*/{},
        /*dns_over_tls_hostnames=*/{}, "https://chromium.dns.nextdns.io",
        /*ui_name=*/"NextDNS",
        /*privacy_policy=*/"https://nextdns.io/privacy",
        /*display_globally=*/false, /*display_countries=*/{"US"},
        LoggingLevel::kNormal},
+       {"Mullvad",
+        MAKE_BASE_FEATURE_WITH_STATIC_STORAGE(DohProviderMullvad,
+                                              base::FEATURE_ENABLED_BY_DEFAULT),
+        /*dns_over_53_server_ip_strs=*/{},
+        /*dns_over_tls_hostnames=*/{}, "https://dns.mullvad.net/dns-query",
+        /*ui_name=*/"Mullvad DNS",
+        /*privacy_policy=*/"https://mullvad.net/en/help/dns-over-https-and-dns-over-tls",
+        /*display_globally=*/true, /*display_countries=*/{},
+        LoggingLevel::kNormal},
+       {"AdGuard",
+        MAKE_BASE_FEATURE_WITH_STATIC_STORAGE(DohProviderAdGuard,
+                                              base::FEATURE_ENABLED_BY_DEFAULT),
+        /*dns_over_53_server_ip_strs=*/{},
+        /*dns_over_tls_hostnames=*/{},
+        "https://dns.adguard-dns.com/dns-query",
+        /*ui_name=*/"AdGuard DNS",
+        /*privacy_policy=*/"https://adguard-dns.io/en/public-dns.html",
+        /*display_globally=*/true, /*display_countries=*/{},
+        LoggingLevel::kNormal},
+       {"ControlD",
+        MAKE_BASE_FEATURE_WITH_STATIC_STORAGE(DohProviderControlD,
+                                              base::FEATURE_ENABLED_BY_DEFAULT),
+        /*dns_over_53_server_ip_strs=*/{},
+        /*dns_over_tls_hostnames=*/{},
+        "https://freedns.controld.com/p0",
+        /*ui_name=*/"Control D (Unfiltered)",
+        /*privacy_policy=*/"https://docs.controld.com/docs/free-dns",
+        /*display_globally=*/true, /*display_countries=*/{},
+        LoggingLevel::kNormal},
        {"OpenDNS",
